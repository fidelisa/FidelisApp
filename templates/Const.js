const Const = {
  API: '<%= api %>',
  PREVIEW: '<%= preview %>',
  APP: '<%= app %>',
  PROVIDER: "<%= provider %>",
  PROVIDER_KEY: "<%= provider_key %>",
  USER: '<%= user_id %>'
}

module.exports = Const;